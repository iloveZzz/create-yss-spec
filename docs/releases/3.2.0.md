# create-yss-spec 3.2.0 源码升级记录

本版将模板技术设计入口统一为 yss-technical-design，并按已确认架构选择 DDD 或传统 MVC。新增 MVC 设计分支、v2 技术设计合同和对应校验；旧 DDD 合同继续显式兼容读取。CLI 命令与 Programmatic API 保持既有协议。

- 固定模板提交：`0347a5f6d100791cf012ef0f4ae3e148eece644a`。
- 快照摘要：`1e76b9ef03e5d885ef8842e9a499d3533bc85ef030003f60965305efd8127675`。
- 包名：`create-yss-spec-3.2.0.tgz`。
- 包完整性：`sha512-tLp64JA/ojeZvJKoj7ZX6RzFiqBRj317T17jciu/Y1PR4gseo8xoVIKO+PP69xZfvO+h214UtoTNYBJdCJ0oqg==`。
- `npm test`：172 项通过、0 失败，执行时 YSS_SPEC_TEMPLATE_REPO 指向该提交的隔离本地 checkout，YSS_SPEC_TEMPLATE_REF 绑定上述提交。
- `npm pack --dry-run --ignore-scripts` 与实际 `npm pack --ignore-scripts`：退出码 0。
- 解包后实际 CLI：init 成功；attach/sync/冲突保护/事务回滚 9 项通过；生成实例的技术设计 29 项通过；Skill 投影和锁文件检查通过。

日期：2026-09-08。本轮按用户授权提交并推送 GitHub 源码，不执行 npm publish。快照目录和 template.snapshot.json 按既有 .gitignore 由固定模板生成，不纳入 Git。CI 和默认同步引用已绑定同一提交。

回滚源码可恢复本版前 CLI 提交 bdf3780；模板功能提交前基线为 c897491。用户项目同步仍保留既有本地修改和回滚保护，不通过版本升级覆盖用户代码。
