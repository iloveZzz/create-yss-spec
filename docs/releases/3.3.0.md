# create-yss-spec 3.3.0 源码升级记录

本版分发阶段 5 实现仓库准备聚合门禁、统一 Project Scaffold Contract v4，以及 DDD、Layered MVC、Frontend 三类受控脚手架能力。`yss-product-lifecycle` 同时收敛为按生命周期注册表、编排合同和 Skill 注册表查询所需上下文的薄路由；CLI 命令与 Programmatic API 保持既有协议。

- 固定模板提交：`e9410b9d451393c0d8f642e5f89b3776abb6b8c2`。
- 模板快照摘要：`45843c962ac48d7a798fc1feddaa6dcbabc2b00cc88825dcd6c244761d8d5aa1`。
- manifest 摘要：`f7520dfdfa388b19ea47cb893c2daab8936019783aa84589f13921e459702280`。
- 包名：`create-yss-spec-3.3.0.tgz`。
- 包大小：`14250677` bytes；解包大小：`75093065` bytes；条目数：`6362`。
- 包完整性：`sha512-cZj5SjHGXfLM3rB1YSFWe8HDU2vfu0i0u1KUDic3ON+ArIUX5wrpUDk5zBvwu0yeUmi0zTKZMVaJuW2rozRCOw==`。
- `pnpm test`：172 项通过、0 失败；测试同步显式绑定 GitHub 上述模板提交。
- 实际 tgz 验收：版本输出为 `3.3.0`，空目录初始化成功，生成实例记录相同 `templateCommit`，新增 scaffold schema、生命周期查询入口和前端脚手架验证入口均已包含且保持可执行。

日期：2026-09-09。本轮按用户授权提交并推送 GitHub 源码，不执行 `npm publish`。快照目录和 `template.snapshot.json` 按既有 `.gitignore` 由固定模板生成，不纳入 Git；默认同步引用已绑定同一提交。

回滚 CLI 源码可恢复到提交 `302f0a36176af709c1a37f1ba1cc7c22ebbf162b`；模板功能提交前基线为 `f31c17c`。用户项目同步仍保留既有本地修改和事务回滚保护，不通过版本升级覆盖用户代码。
