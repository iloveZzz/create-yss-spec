# create-yss-spec 3.3.4 源码升级记录

本版修复项目实例写入与生成型 Skill 锁校验之间的顺序错误。`init`、`attach`、`sync` 现在会在同一文件事务内依据最终 Skill 内容重建 `skills-lock.json`，随后再运行 `verify-project-instance`；生成或校验失败时继续完整回滚。

- 固定模板提交：`46b29114de53cf4d8d92123006b211d7b3a5a142`。
- 新增回归场景：目标仓保留一致的本地 Skill 扩展时，`sync` 自动刷新锁文件且最终 `--check` 通过。
- 真实旧实例复现从 `skills-lock.json is stale` 转为通过锁校验；若 `CONTEXT.md` 仍是旧 schema，则准确报告上下文合同迁移阻塞。
- 模板快照摘要：`01f6cf19181d3dfc8230c5b3aff4aab18097b88d4953b182dc0679a9d80909cf`；manifest 摘要：`d194bb318a47c9c53b04292e97805c563ef7b296c2ca9b507cca498be7f52afe`。
- 包名：`create-yss-spec-3.3.4.tgz`；包大小：`9628568` bytes；解包大小：`45463908` bytes；条目数：`3829`。
- 包完整性：`sha512-UUDaAcWPN+1vJ3a276wSfbgw7WCKBRhHpDiqIohiVUh5+SxrT/jv7HshJix8u3VEFcuJlSjMihSTBnny8d8HXQ==`。
- CLI 全量测试：183 项通过、0 失败。实际 tgz 安装后版本、空目录初始化、Skill 投影和锁校验均通过。

日期：2026-09-10。本轮按用户授权提交并推送 GitHub 源码，不执行 `npm publish`。
