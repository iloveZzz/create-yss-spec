# create-yss-spec 3.3.3 源码升级记录

本版绑定模板运行时投影退役与 GitHub 验证体系升级。CLI 仅分发 `.agents`、Codex、Cursor 与 Pi 的共享 Skill，停止生成 Claude、Qoder、Trae 投影和根 `.mcp.json`，并同步新的模板 CI、兼容矩阵与发布前验证合同。已有项目中的退出分发文件仍按 managed baseline、冲突保留和显式 `--prune` 规则处理。

- 固定模板提交：`70ed7b6b1824067a932371dbeb45c9541d08e375`。
- 模板快照摘要：`46ecd8ece63e285f68be495fd4b2a621177d4d527fe099910d24bd2d21640f05`。
- manifest 摘要：`d194bb318a47c9c53b04292e97805c563ef7b296c2ca9b507cca498be7f52afe`。
- 包名：`create-yss-spec-3.3.3.tgz`。
- 包大小：`9628444` bytes；解包大小：`45463208` bytes；条目数：`3829`。
- 包完整性：`sha512-Fv4lq7mQmunM2qGvG/0oMZKdKZI523KAvACgqCotLUg+hT19pRmyUzT6M03TKyB2bjgE0ToCG19yKhWZds/ANQ==`。
- 固定模板快照执行 CLI 全量测试：182 项通过、0 失败。
- 实际 tgz 验收：版本输出为 `create-yss-spec 3.3.3`，空目录初始化成功，Skill 投影与锁校验通过，生成实例记录相同 `templateCommit` 和 `cliVersion`。

日期：2026-09-10。本轮按用户授权提交并推送 GitHub 源码，不执行 `npm publish`。默认同步引用、模板快照和候选包均绑定同一模板提交。

回滚 CLI 源码可恢复到提交 `797476749b3c19099a614520d06428824e257cc7`；模板功能提交前基线为 `1e49baf`。用户项目同步继续保留本地修改、外部备份和事务回滚保护。
