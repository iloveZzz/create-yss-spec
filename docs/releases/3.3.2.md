# create-yss-spec 3.3.2 源码升级记录

本版绑定模板生命周期门禁收敛、请求分诊、OpenAPI Draft 实质校验和模板验证调度优化，并强化项目实例分发边界。CLI 新增 `sync --prune` 的安全清理语义，README 在初始化后交给项目维护，`.gitignore` 只更新受管规则区，init、attach 和 sync 统一执行项目实例门禁。

- 固定模板提交：`34baac842082db141c81945cc43201b5f7d8e84d`。
- 模板快照摘要：`1854fa2fd2eeae0c0191844220958defef539231ab8be870c58eacfc5bde692d`。
- manifest 摘要：`8c7e5dcbce4c98e1e9137e31acee72bacf590a575f71e903f54c698927712f8b`。
- 包名：`create-yss-spec-3.3.2.tgz`。
- 包大小：`14194810` bytes；解包大小：`74917992` bytes；条目数：`6292`。
- 包完整性：`sha512-OrHDPx5+7eVqdyxs0lzs1w6vjGjZE0BjdtzilpN33KCdw+2sdGNYQmShMdqPgo+26Lb4Oe/ILKX6syGgLAh4wQ==`。
- 固定远端快照执行 `npm run test:prepared`：182 项通过、0 失败。
- 实际 tgz 验收：版本输出为 `create-yss-spec 3.3.2`，Programmatic API v1 可加载，空目录初始化成功，生成实例记录相同 `templateCommit` 和 `cliVersion`。

日期：2026-09-10。本轮按用户授权提交并推送 GitHub 源码，不执行 `npm publish`。快照目录、`template.snapshot.json` 和候选 tgz 按既有 `.gitignore` 不纳入 Git；默认同步引用和 CI 均绑定同一模板提交。

回滚 CLI 源码可恢复到提交 `8e71bc2819b5d0319358bc1dbd83df70d106b756`；模板功能提交前基线为 `ae2a3bd`。用户项目同步保留本地修改、外部备份和事务回滚保护。
